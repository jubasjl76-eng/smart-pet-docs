{
  "name": "smart-pet-docs-site",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.30.3",
    "astro": "^5.1.5",
    "sharp": "^0.33.5"
  }
}
